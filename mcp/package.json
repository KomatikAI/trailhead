{
  "name": "@trailhead/mcp-server",
  "version": "4.2.2",
  "private": true,
  "description": "MCP server exposing Trailhead deployment gate — health checks, risk scoring, DORA metrics, and policy enforcement as tools",
  "type": "module",
  "main": "dist/server.js",
  "bin": {
    "trailhead-mcp": "dist/server.js"
  },
  "scripts": {
    "prebuild": "node ../scripts/copy-shared-src.mjs mcp",
    "build": "tsc",
    "start": "node dist/server.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/node": "^25.8.0",
    "typescript": "^6.0.3"
  }
}
